apiVersion: v1
kind: Service
metadata:
  name: {{ include "assistant.fullname" . }}
  labels:
    {{- include "assistant.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "assistant.selectorLabels" . | nindent 4 }}
