apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  namespace: {{ $.Chart.Name }}
spec:
  rules:
    - host: {{ .Files.Get "gateway.domain.val" }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 5000
    - host: {{ .Files.Get "web.domain.val" }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web
                port:
                  number: 7000
  tls:
    - hosts:
        - {{ .Files.Get "gateway.domain.val" }}
        - {{ .Files.Get "web.domain.val" }}
      secretName: {{ $.Chart.Name }}-tls
